# Cowrieログ差分通知システム 仕様書

## 対象と構成

CowrieのJSON LinesログをSSH経由で読み、新しく観測した値をDiscordへ通知する。常駐せず、OSのスケジューラから定期起動する。抽出はイベントIDと文字列フィールドで指定し、既知の値は累積保持する。再出現・回数増加・削除は通知しない。

実行側はWindowsまたはLinux、Python 3.10以降・uv・OpenSSHを使用する。監視先はPOSIXシェルとPython 3.10以降の `python3` が必要。実行時のPython依存は標準ライブラリのみ。Webhook URLは環境変数から読む。導入・スケジューラ設定はREADMEを参照。

## 設定

`config.json` が運用設定、`config.example.json` が公開例。相対パスは設定ファイルのディレクトリを基準にする。

抽出方式はJSONログの差分取得のみ。旧設定の `notify_removed`・`regex`・`capture_group` は廃止し、運用設定・公開例から削除している。削除通知、grepによる抽出、キャプチャグループによる抽出は行わない。未知の設定キーは現在の読込処理では無視されるため、旧キーを残しても機能は有効にならない。

| キー | 既定値・条件 |
|---|---|
| ssh.host | 必須。SSHホストまたはエイリアス |
| ssh.executable | ssh |
| ssh.timeout_seconds | 90秒 |
| state_file | state/snapshots.json |
| log_file | logs/monitor.log |
| webhook_env | DISCORD_WEBHOOK_URL |
| notify_initial | false。trueなら初回の既存値も通知 |
| checks | 空でない配列 |

各checkの設定:

| キー | 既定値・条件 |
|---|---|
| name | 必須。一意、1行、120文字以内 |
| file | 必須。リモートのJSON Linesファイル |
| json_field | 必須。抽出する文字列フィールド |
| event_ids | 必須。空でないイベントIDの文字列配列 |
| interval_seconds | 300。0以上の整数。0は毎回 |
| mention_everyone | false。trueなら通知の最初のチャンクだけ全員メンション |

公開例の抽出:

| 項目 | event_ids | json_field |
|---|---|---|
| commands | cowrie.command.input | input |
| uploads | cowrie.session.file_download / cowrie.session.file_upload | message |
| user | cowrie.login.failed / cowrie.login.success | username |
| password | cowrie.login.failed / cowrie.login.success | password |
| source-addresses | cowrie.session.connect | src_ip |

uploadsは完了したファイル取得・アップロードの説明文を比較する。転送失敗や任意のSFTP操作は対象外。公開例の間隔は全項目0、メンションは無効。

## 実行と状態

1. 状態ファイルに対応するOSロックを取得する。競合時は正常終了する。
2. 各checkの前回開始時刻と実行間隔を比較する。未到達ならスキップする。
3. 実行開始時刻を保存し、対象ファイルの追記分を取得する。同じファイルは1起動につき1回だけ取得する。
4. イベントを選別し、抽出値を累積観測値へ追加する。読取り位置と観測値を一緒に保存する。
5. 累積観測値から比較用データにある値を除き、追加分を通知する。初回はnotify_initialに従い、falseなら通知せず基準値として保存する。
6. 通知がすべて成功したcheckの比較用データを更新する。

状態ファイルのversionは1。`last_runs` は各checkの開始時刻、`streams` はファイルの読取り位置、`observed` は累積観測値、`checks` は通知成功時の比較用データを保持する。状態は一時ファイルのflush・fsync後に置き換える。不正な状態は上書きしない。既存状態に追加フィールドがなければ空として読み、既存の比較用データを引き継ぐ。

抽出条件の識別値はSSHホスト・ファイル・固定文字列json・json_field・重複排除してソートしたevent_idsの配列からSHA-256で生成する。既存JSON設定の識別値は維持する。設定変更時は該当checkの基準値を作り直す。間隔だけの変更では基準値を維持する。

失敗した実行も開始時刻を記録し、次の間隔到達後に再試行する。時計が戻った場合は即実行する。停止中の回数分の追いつき実行はしない。`--dry-run` は同じ間隔判定・取得・解析を行うが状態更新や通知は行わない。

## 入出力と失敗時の動作

SSHはBatchModeとStrictHostKeyCheckingを有効にし、ConnectTimeoutは15秒、ServerAliveIntervalは15秒、ServerAliveCountMaxは2。終了コード0のみ成功とする。

ログはUTF-8のJSON Linesとして検証する。空行を除き、eventidは文字列必須。対象イベントの抽出フィールドも文字列必須。空文字、空白、スラッシュ、JSONでエスケープされた改行を保持する。取得や解析に失敗したファイルの読取り位置・観測値は更新せず、同じファイルを参照するcheckを失敗とする。他ファイルの処理は継続する。

通知は追加値を `+` で示し、長い内容を分割する。ログ由来のメンションを無効化し、バッククォートを表示用に置換する。HTTP 429 / 5xxは最大3回試行し、各待機は30秒以内。途中まで送信して失敗した場合、再送で重複することがある。

終了コードは通常終了・重複起動スキップで0、設定・取得・解析・通知の失敗で1。実行ログは2MBでローテーションし、バックアップを3本保持する。

## JSONログの差分取得とローテーション

- SSH先の `python3`（3.10以降、標準ライブラリのみ）が必要です。リモートへファイルを設置せず、同梱の `incremental_reader.py` をSSH経由で実行します。
- 初回は現在ファイルを読みます。既存の比較用データは引き継ぎます。以後はデバイス・inode・バイト位置と直前256バイトのSHA-256を用いて続きだけ取得します。
- 同じファイルを参照する全JSON項目へ取得結果を振り分けます。通知間隔未到達の項目も観測値を保存し、通知は各項目の間隔に従います。
- `streams` に読取り位置、`observed` に累積観測値を一緒に永続保存してから通知します。`checks` は通知成功時に更新するため、再起動・通知失敗後も未通知の値を再送できます。部分送信後の失敗では通知が重複する場合があります。
- 同じディレクトリに元のファイル名で始まる未圧縮の退避ファイルが残っていれば、旧inodeを探し、更新日時順に未読部分を回収して新ファイルへ進みます。初回は過去の退避ファイルを遡りません。
- 旧世代が圧縮・削除済み、別ディレクトリへ移動済み、または名前の接頭辞が異なる場合は取得エラーとし、読取り位置を保持します。退避ファイルの更新日時が世代順を表す運用を前提とします。監視の停止期間より長く未圧縮の世代を残してください。
- 同じinodeのサイズ縮小や直前部分の変更を検出すると、警告して先頭から再開します。copytruncateのコピーと切詰めの間の欠落や、同一内容で再伸長した場合の検出は保証できません。
- JSONの末尾の改行未完了行は次回に持ち越します。完成済み行が不正な場合、または退避ファイルの最終行が未完了の場合は取得を失敗させ、読取り位置を保持します。
- 観測値は累積するため、削除通知は行いません。既知の値の再出現も通知しません。状態ファイルは値の種類に応じて増えます。
- 抽出条件の変更・項目追加では、そのファイルを先頭から再読取りします。変更していない項目の累積値は維持します。`--dry-run` は取得のみ行い、永続状態や通知を変更しません。
